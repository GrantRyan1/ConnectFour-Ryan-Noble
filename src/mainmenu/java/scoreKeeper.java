/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package mainmenu.java;

/**
 *
 * @author Ryan
 * 
 */
public class scoreKeeper {
    
    /**
     *
     * @param args
     */
    public static void main(String[] args); {       

    
            
    scoreStats(); {

 int gameStatistics;
    if( wins<0); {
        println("Error, wins must be >=0");
        
     if( losses<0);
        println("Error, losses must be >=0");

     if( ties<0);
        println("Error, ties must be >=0");
}
      if else;
          return gameStatistics;

      totalGames / totalWins = winStats;

      println("winStats + totalWins + totalLosses + totalTies");
      println("highScores");

    }
   }
  }
